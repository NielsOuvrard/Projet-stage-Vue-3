export type Logged = {
    email: string
    password: string
}

export type Registered = {
    firstName: string
    lastName: string
    email: string
    password: string
    confirmPassword: string
}
